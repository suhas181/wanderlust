<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WanderLust</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Navigation Bar Styling */
        .navbar-custom {
            background: #ff6b6b; /* Soft Red Gradient */
            padding: 15px 20px;
        }
        .navbar-brand {
            font-weight: bold;
            color: white !important;
        }
        .nav-link {
            color: white !important;
            font-size: 18px;
            transition: 0.3s ease-in-out;
        }
        .nav-link:hover {
            color: #ffe66d !important; /* Soft Yellow Hover Effect */
        }
        .nav-active {
            font-weight: bold;
            border-bottom: 3px solid #ffe66d;
        }
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-custom">
        <div class="container">
            <a class="navbar-brand" href="/">🏠 WanderLust</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link nav-active" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/listings">All Listings</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/listings/new">➕ Add New Listing</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4 d-flex justify-content-center">
        <!-- Listing Card -->
        <div class="card border-0 text-center" style="max-width: 600px;">
            <% if (listing.image) { %>
                <img src="<%= listing.image.url %>" class="rounded img-fluid mx-auto d-block" 
                    style="max-width: 500px; height: auto;" alt="Listing Image">
            <% } else { %>
                <p class="text-center text-muted">No image available.</p>
            <% } %>

            <div class="card-body">
                <h2 class="card-title fw-bold"><%= listing.title %></h2>
                <p class="card-text text-muted"><%= listing.description %></p>
                
                <p><strong>Location:</strong> <%= listing.location %>, <%= listing.country %></p>
                <p><strong>Price:</strong> ₹<%= listing.price.toLocaleString("en-IN") %></p>

                <!-- Edit & Delete Buttons -->
                <div class="d-flex justify-content-center">
                    <a href="/listings/<%= listing._id %>/edit" class="btn btn-danger me-2">Edit</a>
                    <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE">
                        <button type="submit" class="btn btn-dark">Delete</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="text-center mt-5">
        <p>© Wanderlust Private Limited</p>
        <div>
            <a href="#"><i class="bi bi-facebook"></i></a>
            <a href="#"><i class="bi bi-instagram"></i></a>
            <a href="#"><i class="bi bi-youtube"></i></a>
        </div>
        <a href="#">Privacy</a> | <a href="#">Terms</a>
    </footer>

    <!-- Bootstrap JS (Optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
